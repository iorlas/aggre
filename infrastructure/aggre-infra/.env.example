# Tailscale auth key (generate at https://login.tailscale.com/admin/settings/keys)
TS_AUTHKEY=tskey-auth-...

# PostgreSQL
POSTGRES_USER=aggre
POSTGRES_PASSWORD=changeme
POSTGRES_DB=aggre

# Garage admin token (generated during garage setup)
GARAGE_ADMIN_TOKEN=
