\* Add statements after this line.
CONSTANTS
    NumItems = 3
    PermanentHNFailItem = 1

SPECIFICATION Spec

\* Safety invariant that should PASS
INVARIANT SensorGuard

\* This invariant should FAIL -- demonstrates the infinite reprocessing bug.
\* Item 1 has PermanentHNFailItem=1, so HN always fails for it.
\* The partial failure bug means enriched_at stays NULL, and the item is
\* re-queried indefinitely. TLC will find processCount[1] > 2.
INVARIANT NoInfiniteReprocess
