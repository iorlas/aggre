# Production setup: requires an external Hatchet server.
# Set HATCHET_CLIENT_TOKEN in .env to connect.
x-app-env: &app-env
  AGGRE_DATABASE_URL: postgresql+psycopg://aggre:aggre@postgres/aggre
  HATCHET_CLIENT_TOKEN: ${HATCHET_CLIENT_TOKEN:-}
  HATCHET_CLIENT_TLS_STRATEGY: none

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aggre
      POSTGRES_USER: aggre
      POSTGRES_PASSWORD: aggre
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aggre"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrate:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run alembic upgrade head
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    environment:
      <<: *app-env
    env_file:
      - path: .env
        required: false
    depends_on:
      postgres:
        condition: service_healthy

  hatchet-worker:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run python -m aggre.workflows
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    environment:
      <<: *app-env
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully

  tor-proxy:
    image: peterdavehello/tor-socks-proxy:latest
    restart: unless-stopped
