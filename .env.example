# Aggre configuration
# Copy to .env and adjust as needed

# PostgreSQL connection URL (default: postgresql://localhost/aggre)
# AGGRE_DATABASE_URL=postgresql://localhost/aggre

# Log directory (default: ./data/logs)
# AGGRE_LOG_DIR=./data/logs

# Whisper model for YouTube transcription (default: large-v3)
# AGGRE_WHISPER_MODEL=large-v3

# Whisper model cache directory (default: ./data/models)
# AGGRE_WHISPER_MODEL_CACHE=./data/models

# YouTube temp video directory (default: ./data/tmp/videos)
# AGGRE_YOUTUBE_TEMP_DIR=./data/tmp/videos

# Reddit rate limit in seconds between requests (default: 3.0)
# AGGRE_REDDIT_RATE_LIMIT=3.0

# Max items to fetch per source per poll (default: 100)
# AGGRE_FETCH_LIMIT=100
