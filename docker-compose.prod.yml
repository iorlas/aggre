services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aggre
      POSTGRES_USER: aggre
      POSTGRES_PASSWORD: aggre
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aggre"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrate:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run alembic upgrade head
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - path: .env
        required: false
    depends_on:
      postgres:
        condition: service_healthy

  collect:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run aggre collect --loop --interval 3600
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully

  download:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run aggre download --loop --interval 10
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully

  extract-html-text:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run aggre extract-html-text --loop --interval 10
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully

  enrich-content-discussions:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run aggre enrich-content-discussions --loop --interval 60
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully

  transcribe:
    image: ghcr.io/iorlas/aggre:latest
    command: uv run aggre transcribe --loop --interval 10
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully

  tor-proxy:
    image: peterdavehello/tor-socks-proxy:latest
    restart: unless-stopped
